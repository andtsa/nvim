return {
    'nvim-java/nvim-java',
    config = function()
        -- require("java").setup({
        --     spring_boot_tools = {
        --         enable = false,
        --     },
        -- })
    end
}
